// describe('Initialization script', () => {
//   it ('checks if device is connected to the internet', () => {})
//   it ('checks if server is reachable', () => {})
//   it ('refreshes user token', () => {})
// })