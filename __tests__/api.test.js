// describe('API error process', () => {
//   it ('logs the error when api error occurs', () => {})
// })
//
// describe('Refreshment of user token', () => {
//   it ('refreshes the token after any call if it is too old', () => {})
//   it ('keeps the token after any call if it is still young', () => {})
// })