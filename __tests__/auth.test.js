// describe('Email registration process', () => {
//   it ('saves access token and redirects to home when registering with email', () => {})
//   it ('does not register if email or password is not provided', () => {})
//   it ('does not register if email already exists', () => {}) // displays proper error
// })
//
// describe('Email login process', () => {
//   it ('saves access token and redirects to home when logging with email', () => {})
//   it ('does not login if email or password is not provided', () => {})
//   it ('does not login if password is wrong', () => {})
// })
//
// describe('App logout process', () => {
//   it ('removes information from state and redirect to AuthMain when logging out', () => {})
// })
//
// describe('Facebook authentication process', () => {
//   it ('saves access token and redirects to home when authenticating with facebook', () => {})
// })